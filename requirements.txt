# =====================
# 基本機能
# =====================
Flask==2.3.3
gunicorn==21.2.0  # 最新安定版（20.1.0 → 21.2.0 に更新推奨）
line-bot-sdk==3.8.0
openai==1.33.0  # 最新安定版（1.31.1 → 1.33.0）
python-dotenv==1.0.1
requests==2.31.0
pytz==2024.1
certifi==2024.6.2
tqdm==4.66.4
tenacity==8.2.3

# =====================
# ファイル処理・画像処理
# =====================
pillow==10.3.0
numpy==1.26.4
dropbox==11.36.2

# =====================
# OCR（画像文字認識）
# =====================
easyocr==1.7.1
scikit-image==0.22.0
torch==2.3.0
torchvision==0.18.0
opencv-python-headless==4.9.0.80

# =====================
# 将来の拡張（軽量 or 無効化可能）
# =====================
python-magic==0.4.27              # ファイルタイプ判定
moviepy==1.0.3                    # 動画解析（Render本番では非推奨）
google-api-python-client==2.125.0  # Google Sheets API対応（認証別途）